 - Create the database (keep an updated dump file on the project folder):
	- Users table
	- More to come......

 - Create a footer.php to import in all files. <done>

 - Create a head.php to import in all files.

 - Create a menu.php to import in "all" files. If in all we could merge it in the head.php file later.

 - Create a db.php with the connection details to simplify.

 - Create a functions.php to keep the custom functions used in the project. Maibe more than 1 file i can.